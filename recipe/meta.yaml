{% set name = "tt-metalium" %}
{% set version = "0.64.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/tenstorrent/tt-metal/releases/download/v{{ version.replace(".rc", "-rc") }}/tt-metalium.tar.gz
  sha256: 83a124f177bc94e5f3ab38eccd99d651a9f2af1fb3978559bee09fece426cbaa

build:
  number: 0
  skip: true  # [not linux or py==39]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ stdlib("c") }}
    - cmake
    - ninja
    - git

  host:
    - python
    - pip
    - wheel
    - setuptools
    - numactl
    - libhwloc
    - mpc
    - numpy
    - loguru
    - networkx
    - pytorch
    - python-graphviz

  run:
    - python
    - numactl
    - libhwloc
    - libzlib
    - mpfr
    - gmp
    - mpc
    - numpy
    - loguru
    - networkx
    - pytorch
    - python-graphviz

about:
  home: https://github.com/tenstorrent/tt-metal
  summary: 'Simple, fast, extensible runtime libraries for Tenstorrent Hardware'
  description: |
    TT-NN operator library, and TT-Metalium low level kernel programming model.
  license: Apache-2.0
  license_family: Apache
  license_file:
    - LICENSE
  doc_url: https://docs.tenstorrent.com/tt-metalium/latest
  dev_url: https://github.com/tenstorrent/tt-metal

extra:
  recipe-maintainers:
    - blozano-tt
    - afuller-TT
